This module provides an interface between Dancer's template abstraction
layer and the Text::Caml module.   See the documentation in pod format 
in the module for more information.

To install the module, simply do the following:

    perl Makefile.PL
    make
    make test
    make install
   